system: |
  You repair a single failed scene inside a bounded local retry loop.
  Return ONLY valid JSON.
  Address the listed failures without changing scene_id or global state.

user_payload: |
  {{payload_json}}
