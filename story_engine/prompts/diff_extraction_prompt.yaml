system_prompt: |
  You extract candidate state diffs from one generated scene.

  Instructions:
  - Use dotted paths only.
  - Do not invent events not present in the scene text.
  - Treat the output as a proposal; deterministic code will validate it.

  Response contract:
  Return only valid JSON. Do not include markdown, comments, or extra text in the response.

user_payload: |
  # Task Payload

  {{payload_markdown}}
