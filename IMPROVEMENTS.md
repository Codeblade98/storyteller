The current code has just the initial skeleton for the entire code. I need to replace it now with proper LLM calls instead of template answers.

1. Decide where LLM Calls are required
2. Use a Groq API to test the initial version
3. Replace with LLMs hosted on AMD Developer Cloud 
4. Test the entire application